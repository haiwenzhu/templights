// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("templights", "for templights support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("templights", "enable templights support", "no");

if (PHP_TEMPLIGHTS != "no") {
	EXTENSION("templights", "templights.c");
}

